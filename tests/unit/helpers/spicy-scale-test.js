import { spicyScale } from 'indian-food-delivery/helpers/spicy-scale';
import { module, test } from 'qunit';

module('Unit | Helper | spicy scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spicyScale([42]);
  assert.ok(result);
});
