{{#if addNewMealForm}}
  <form class="form-group new-meal">
    <h2>Create a New Meal Item</h2>
    <div>
      <label>Name</label>
      {{input required value=name id="name" class="form-control"}}
    </div>
    <div>
      <label>Description</label>
      {{textarea required value=description id="description" class="form-control"}}
    </div>
    <div>
      <label>Price</label>
      {{input required value=price id="price" class="form-control" type="number"}}
    </div>
    <div>
      <label>Ingredients</label>
      {{textarea required value=ingredients id="ingredients" class="form-control"}}
    </div>
    <div>
      <label>Spicy Scale (1-5)</label>
      {{input required value=spicyScale id="spicyScale" class="form-control"}}
    </div>
    <div>
      <label>Image URL</label>
      {{input required value=imgURL id="imgURL" class="form-control"}}
    </div>
    <div>
      {{input required value=popularity id="popularity" class="form-control" type="hidden"}}
    </div>
    <button {{action "saveMeal"}} class="btn btn-success">Submit</button>
  </form>
{{else}}
  <button {{action "showNewMealForm"}} class="btn new-meal-btn btn-primary">Create New Meal</button>
{{/if}}
